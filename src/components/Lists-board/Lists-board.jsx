import React from "react";
import ListBoard from '../List-board/List-board.jsx';

const ListsBoard = (props) => {
  return (
    <div className="page-main__list lists row">
      <ListBoard listData = {{}} />
    </div>
  );
};

export default ListsBoard;
